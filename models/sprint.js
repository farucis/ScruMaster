class Sprint {
    constructor(sprintId,projectId,number,time) {
        this.sprintId = sprintId;
        this.projectId = projectId;
        this.number = number;
        this.time = time;
    }
}

export default Sprint;